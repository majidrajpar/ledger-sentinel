[package]
name = "audit-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Pure analytics engine for continuous controls and forensic ledger testing"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
